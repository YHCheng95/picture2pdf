# 图片转 PDF 工具 - 设计指南

## 产品定位

**核心功能**：多图上传 → PDF 生成 → 一键下载

**目标用户**：需要将多张图片快速转换为 PDF 文档的用户（如证件扫描、资料整理、合同归档等）

**设计风格**：简洁专业、高效直观、工具属性强

---

## 气质与意象

**关键词**：高效、专业、简洁、可靠

**场景描述**：
清晨的办公室，白色桌面干净整洁，一叠纸质文件整齐排列。用户打开应用，快速拍照或选择图片，一键转换，PDF 文件即刻生成。整个过程如流水般顺畅，没有多余的装饰，只有清晰的步骤指引和即时的反馈。

---

## 视觉策略

### 配色方案

**主色调**：深蓝色 `#2563eb`（专业、可靠、信任感）
- Tailwind 类名：`bg-blue-600` / `text-blue-600`

**辅助色**：
- 成功绿：`#10b981`（转换成功提示）→ `bg-green-500` / `text-green-500`
- 警告橙：`#f59e0b`（操作提示）→ `bg-orange-400` / `text-orange-400`
- 错误红：`#ef4444`（错误提示）→ `bg-red-500` / `text-red-500`

**中性色**：
- 深灰文字：`#171717` → `text-neutral-900`
- 次级文字：`#737373` → `text-neutral-500`
- 边框线：`#e5e5e5` → `border-neutral-200`
- 背景色：`#fafafa` → `bg-neutral-50`

### 字体排版

**标题字体**：
- H1（页面标题）：`text-2xl font-bold`
- H2（区块标题）：`text-xl font-semibold`
- H3（卡片标题）：`text-lg font-medium`

**正文字体**：
- 正文：`text-base`
- 辅助文字：`text-sm`
- 提示文字：`text-xs`

---

## 间距系统

**页面边距**：`px-4`（左右各 16px）
**卡片内边距**：`p-4`（16px）
**组件间距**：`gap-4`（16px）
**列表项间距**：`gap-3`（12px）

---

## 组件使用原则

**强制约束**：
- 按钮、输入框、卡片、弹窗、Toast 等通用组件必须优先使用 `@/components/ui/*`
- 禁止用 `View/Text` 手搓通用 UI 组件
- 页面结构容器使用 `@tarojs/components` 的 `View/Text`

**主要组件选型**：
- 按钮：`Button`（`@/components/ui/button`）
- 卡片：`Card`（`@/components/ui/card`）
- 提示：`Toast`（`@/components/ui/toast`）
- 加载态：`Progress`（`@/components/ui/progress`）
- 空状态：使用 `Text` + 图标组合，不单独造组件

---

## 页面结构

### 首页布局（单页面应用）

```
┌─────────────────────────────────┐
│         顶部标题栏               │
│    "图片转 PDF 工具"             │
├─────────────────────────────────┤
│                                 │
│      图片上传区域（拖拽/点击）    │
│      [+] 添加图片                │
│                                 │
├─────────────────────────────────┤
│                                 │
│      已选图片列表（可排序）       │
│      ┌─────┐ ┌─────┐ ┌─────┐   │
│      │ 图片 │ │ 图片 │ │ 图片 │   │
│      └─────┘ └─────┘ └─────┘   │
│      [删除]  [删除]  [删除]       │
│                                 │
├─────────────────────────────────┤
│                                 │
│      [转换并下载 PDF]            │
│                                 │
└─────────────────────────────────┘
```

### 交互流程

1. **上传图片**：
   - 点击"添加图片"按钮
   - 调用 `Taro.chooseImage()` 选择多张图片
   - 显示上传进度
   - 上传到对象存储，获取 URL

2. **管理图片**：
   - 显示已选图片缩略图列表
   - 支持删除单张图片
   - 显示图片顺序编号

3. **生成 PDF**：
   - 点击"转换并下载"按钮
   - 显示转换进度
   - 调用后端接口生成 PDF
   - 返回 PDF 下载链接

4. **下载 PDF**：
   - 使用 `Taro.downloadFile()` 下载 PDF
   - 保存到本地

---

## 导航结构

**单页面应用**：无需 TabBar，无需多页面跳转

**路由配置**：
- `pages/index/index`：首页（图片上传 + PDF 转换）

---

## 小程序约束

### 包体积优化

- TabBar 图标：无（单页面）
- 其他图片：使用对象存储 URL
- PDF 生成：后端处理，前端仅下载

### 性能优化

- 图片压缩：前端上传前压缩（如超过 1MB）
- 上传并发：限制同时上传 3 张
- PDF 下载：使用临时文件路径，避免占用本地存储

### 跨端兼容

- 图片上传：使用 `Network.uploadFile()`
- 文件下载：使用 `Taro.downloadFile()`
- 平台检测：支持 H5 和小程序

---

## 设计禁忌

**禁止事项**：
- ❌ 过度装饰：不要添加背景图案、渐变等装饰元素
- ❌ 动画过多：只保留必要的加载动画
- ❌ 颜色过多：严格控制配色，不要使用彩虹色
- ❌ 文字过小：最小字号 12px（`text-xs`）
- ❌ 无反馈：任何操作必须有明确的视觉反馈（加载、成功、失败）